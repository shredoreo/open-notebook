```ｊｓ

／／限制输入
restrictQty(value) {
  if (+value > +this.modifyingStaff.remainder) {
    this.modedQty = this.modifyingStaff.remainder
  }
},
```





```vue
//replace + back 清除历史记录
this.$router.replace({
  name: this.from || 'GxtOperation',
  params: {
    transferqty, operator, workreportid
  }
})
this.$router.back();
```

